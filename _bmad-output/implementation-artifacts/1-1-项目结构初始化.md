# Story 1.1：项目结构初始化

Status: done

## Story

作为操作者，
我希望按照架构规范初始化完整的项目目录结构、基础文件和依赖管理，
以便能在新机器上一键安装并获得可运行的工具脚手架。

## Acceptance Criteria

1. **Given** 一个空目录，**When** 执行 `pip install -r requirements.txt` 后再执行 `playwright install chromium`，**Then** 所有依赖安装成功无报错，且可执行 `python validate.py --help`
2. **Given** 项目初始化完成，**When** 检查目录结构，**Then** 以下文件/目录全部存在：
   - `validate.py`（空 CLI 骨架，`--help` 可执行）
   - `reporter.py`（空骨架）
   - `requirements.txt`（含 `python-dotenv requests playwright PyYAML pytest`）
   - `.env.example`（列出所有必填凭证键名）
   - `.gitignore`（明确排除 `.env`、`__pycache__/`、`*.pyc`、`.pytest_cache/`）
   - `README.md`（含安装步骤 + Playwright 说明）
   - `config/__init__.py`
   - `config/credentials.py`（骨架，含 `get_credentials()` 函数定义）
   - `config/field_requirements.yaml`（骨架，含利润表 profit_table 至少 3 条示例字段）
   - `sources/__init__.py`
   - `reports/`（空目录，含 `.gitkeep`）
   - `tests/__init__.py`
   - `tests/conftest.py`（骨架，含 `mock_credentials` fixture）
   - `tests/fixtures/`（空目录，含 `.gitkeep`）
3. **Given** 项目被克隆到新机器，**When** 检查 `.gitignore`，**Then** 明确排除 `.env`、`__pycache__/`、`*.pyc`、`.pytest_cache/`
4. **Given** 读取 `README.md`，**When** 查看安装步骤，**Then** 包含：Python 3.x 环境要求、`pip install -r requirements.txt`、`playwright install chromium`（附注该步骤无法通过 requirements.txt 自动完成）、各数据源运行命令示例（如 `python validate.py --source triplewhale`、`python validate.py --all`）

## Tasks / Subtasks

- [x] 创建根目录文件（AC: #1 #2）
  - [x] 创建 `validate.py`：argparse 骨架，支持 `--source` 和 `--all` 参数，`--help` 可用，`main()` 为 pass
  - [x] 创建 `reporter.py`：模块骨架，含 `write_raw_report()` 和 `init_validation_report()` 函数定义（pass 实现）
  - [x] 创建 `requirements.txt`：写入 `python-dotenv\nrequests\nplaywright\nPyYAML\npytest`
  - [x] 创建 `.env.example`：列出全部必填凭证键（见 Dev Notes）
  - [x] 创建 `.gitignore`：排除 `.env`、`__pycache__/`、`*.pyc`、`.pytest_cache/`（AC: #3）
  - [x] 创建 `README.md`（AC: #4）

- [x] 创建 `config/` 目录（AC: #2）
  - [x] 创建 `config/__init__.py`（空文件）
  - [x] 创建 `config/credentials.py`（骨架，见 Dev Notes）
  - [x] 创建 `config/field_requirements.yaml`（骨架，含 profit_table 3 条示例字段）

- [x] 创建 `sources/` 目录（AC: #2）
  - [x] 创建 `sources/__init__.py`（空文件）

- [x] 创建 `reports/` 目录（AC: #2）
  - [x] 创建 `reports/.gitkeep`（保证空目录被 Git 追踪）

- [x] 创建 `tests/` 目录（AC: #2）
  - [x] 创建 `tests/__init__.py`（空文件）
  - [x] 创建 `tests/conftest.py`（骨架，含 `mock_credentials` fixture）
  - [x] 创建 `tests/fixtures/`（空目录）
  - [x] 创建 `tests/fixtures/.gitkeep`

## Dev Notes

### 必须创建的完整文件清单及内容规范

#### `validate.py` 骨架

```python
"""outdoor-data-validator — 统一 CLI 验证入口"""
import argparse
import sys


def main() -> None:
    parser = argparse.ArgumentParser(
        description="outdoor-data-validator: 验证各数据源 API 接入与字段发现"
    )
    parser.add_argument("--source", type=str, help="指定单个数据源名称（如 triplewhale）")
    parser.add_argument("--all", action="store_true", help="运行全部数据源的验证")
    args = parser.parse_args()

    if not args.source and not args.all:
        parser.print_help()
        sys.exit(1)

    # TODO: Story 5.1 实现调度逻辑


if __name__ == "__main__":
    main()
```

#### `reporter.py` 骨架

```python
"""报告渲染器：负责 raw.md 写入和 validation.md 首次创建"""
from pathlib import Path
from typing import Any


def write_raw_report(
    source_name: str,
    fields: list[dict],
    table_name: str | None = None,
    sample_count: int = 0,
) -> None:
    """写入 reports/{source_name}-raw.md，每次覆盖。"""
    # TODO: Story 1.4 实现
    pass


def init_validation_report(source_name: str) -> None:
    """首次创建 reports/{source_name}-validation.md，已存在则不覆盖。"""
    # TODO: Story 1.4 实现
    pass
```

#### `config/credentials.py` 骨架

```python
"""凭证加载器：唯一允许读取 .env 的位置。所有 source 模块从此导入。"""
import os
from dotenv import load_dotenv

load_dotenv()

# 所有 source 模块必须从此导入凭证，禁止直接调用 os.getenv()
_REQUIRED_KEYS: list[str] = [
    "TRIPLEWHALE_API_KEY",
    "TIKTOK_REFRESH_TOKEN",
    "TIKTOK_APP_KEY",
    "TIKTOK_APP_SECRET",
    "DINGTALK_APP_KEY",
    "DINGTALK_APP_SECRET",
    "YOUTUBE_API_KEY",
    "AWIN_USERNAME",
    "AWIN_PASSWORD",
    "CARTSEE_USERNAME",
    "CARTSEE_PASSWORD",
    "PARTNERBOOST_USERNAME",
    "PARTNERBOOST_PASSWORD",
]


def get_credentials() -> dict[str, str]:
    """加载并校验所有必需凭证，缺失时抛出 ValueError。

    Returns:
        包含所有凭证键值的字典

    Raises:
        ValueError: 当一个或多个必需凭证键缺失时
    """
    creds: dict[str, str] = {}
    missing: list[str] = []

    for key in _REQUIRED_KEYS:
        value = os.getenv(key)
        if value is None:
            missing.append(key)
        else:
            creds[key] = value

    if missing:
        raise ValueError(f"缺少以下必需凭证：{', '.join(missing)}")

    return creds
```

> **重要**：Story 1.2 将完善 `get_credentials()`，本 Story 只需创建骨架。

#### `config/field_requirements.yaml` 骨架

```yaml
# 字段需求配置：按报表分组，每条记录含 display_name / source / table 三字段
# table 对非 SQL 数据源为 null（可选字段）

profit_table:  # 利润表（04-08 截止）
  - display_name: 日期
    source: triplewhale
    table: pixel_orders_table
  - display_name: 销售额
    source: triplewhale
    table: pixel_orders_table
  - display_name: SKU
    source: tiktok
    table: orders

marketing_table:  # 营销表现表（04-20 截止）
  - display_name: 曝光量
    source: triplewhale
    table: pixel_joined_tvf
```

#### `.env.example`

```
# outdoor-data-validator 凭证模板
# 复制此文件为 .env 并填入真实凭证

# TripleWhale
TRIPLEWHALE_API_KEY=

# TikTok Shop（首次需手动完成 OAuth 授权获取 refresh_token）
TIKTOK_REFRESH_TOKEN=
TIKTOK_APP_KEY=
TIKTOK_APP_SECRET=

# 钉钉多维表
DINGTALK_APP_KEY=
DINGTALK_APP_SECRET=

# YouTube
YOUTUBE_API_KEY=

# Awin（账号密码登录）
AWIN_USERNAME=
AWIN_PASSWORD=

# CartSee（账号密码登录）
CARTSEE_USERNAME=
CARTSEE_PASSWORD=

# PartnerBoost（账号密码登录）
PARTNERBOOST_USERNAME=
PARTNERBOOST_PASSWORD=
```

#### `tests/conftest.py` 骨架

```python
"""pytest 全局 fixtures：mock_credentials 供所有单元测试使用"""
import pytest
from unittest.mock import patch


TEST_CREDENTIALS: dict[str, str] = {
    "TRIPLEWHALE_API_KEY": "test_tw_key",
    "TIKTOK_REFRESH_TOKEN": "test_tiktok_refresh",
    "TIKTOK_APP_KEY": "test_tiktok_app_key",
    "TIKTOK_APP_SECRET": "test_tiktok_app_secret",
    "DINGTALK_APP_KEY": "test_dingtalk_app_key",
    "DINGTALK_APP_SECRET": "test_dingtalk_app_secret",
    "YOUTUBE_API_KEY": "test_youtube_key",
    "AWIN_USERNAME": "test_awin_user",
    "AWIN_PASSWORD": "test_awin_pass",
    "CARTSEE_USERNAME": "test_cartsee_user",
    "CARTSEE_PASSWORD": "test_cartsee_pass",
    "PARTNERBOOST_USERNAME": "test_pb_user",
    "PARTNERBOOST_PASSWORD": "test_pb_pass",
}


@pytest.fixture
def mock_credentials():
    """Mock get_credentials() 返回测试凭证字典，无需真实 .env 文件。

    用法：
        def test_something(mock_credentials):
            # get_credentials() 已被 mock，返回 TEST_CREDENTIALS
    """
    with patch("config.credentials.get_credentials", return_value=TEST_CREDENTIALS):
        yield TEST_CREDENTIALS
```

#### `README.md` 内容要点

README 必须包含：
1. 项目简介（一句话）
2. **环境要求**：Python 3.10+
3. **安装步骤**：
   ```bash
   pip install -r requirements.txt
   playwright install chromium  # 注意：此步骤无法通过 requirements.txt 自动完成，必须手动执行
   cp .env.example .env         # 复制凭证模板，填入真实凭证
   ```
4. **运行命令示例**：
   ```bash
   python validate.py --source triplewhale   # 运行单个数据源
   python validate.py --all                  # 运行全部数据源
   python validate.py --help                 # 查看帮助
   ```
5. **输出说明**：报告生成于 `reports/` 目录

### Project Structure Notes

本 Story 需在项目根目录（`BACKEND_ROOT`）创建以下完整结构：

```
outdoor-data-validator/
├── validate.py               ← 空 CLI 骨架
├── reporter.py               ← 空骨架
├── requirements.txt          ← 5 个依赖
├── .env.example              ← 13 个凭证键
├── .gitignore
├── README.md
├── config/
│   ├── __init__.py
│   ├── credentials.py        ← get_credentials() 骨架
│   └── field_requirements.yaml  ← 注意：下划线，不是连字符
├── sources/
│   └── __init__.py
├── reports/
│   └── .gitkeep
└── tests/
    ├── __init__.py
    ├── conftest.py           ← mock_credentials fixture
    └── fixtures/
        └── .gitkeep
```

> **命名注意**：配置文件使用 `field_requirements.yaml`（下划线），架构文档数据架构示例节曾误写为 `field-requirements.yaml`（连字符），以目录树定义为权威命名。[Source: architecture.md#Project Structure & Boundaries]

### 关键约束

- `requirements.txt` 依赖精确为：`python-dotenv`、`requests`、`playwright`、`PyYAML`、`pytest`，不多不少
- `credentials.py` 中凭证统一通过 `get_credentials()` 暴露，source 模块不直接调用 `os.getenv()`
- `.env` 必须在 `.gitignore` 中排除（安全要求 NFR1/NFR3）
- `reports/` 和 `tests/fixtures/` 均为空目录，用 `.gitkeep` 保证 Git 追踪
- Story 范围：骨架实现，所有业务逻辑函数体为 `pass`，不实现实际功能

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 1.1]
- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure & Boundaries]
- [Source: _bmad-output/planning-artifacts/architecture.md#Authentication & Security]
- [Source: _bmad-output/planning-artifacts/architecture.md#Tool Library Decisions]
- [Source: _bmad-output/planning-artifacts/architecture.md#Enforcement Guidelines]

### Review Findings

- [x] [Review][Patch] 空字符串凭证绕过验证检查 [config/credentials.py:28] — 已修复：`if value is None` → `if not value`
- [x] [Review][Patch] `load_dotenv()` 应使用显式路径以保证确定性 [config/credentials.py:7] — 已修复：改为 `load_dotenv(dotenv_path=Path(__file__).parent.parent / ".env")`
- [x] [Review][Patch] `mock_credentials` fixture 不能隔离 `load_dotenv` 副作用 [tests/conftest.py:9] — 已修复：同时 patch `config.credentials.load_dotenv`
- [x] [Review][Patch] `_REQUIRED_KEYS` 与 `TEST_CREDENTIALS` 键集缺乏同步检验 [tests/test_structure.py] — 已修复：新增 `TestCredentialKeysSync::test_test_credentials_keys_match_required_keys`
- [x] [Review][Patch] `.env.example` 缺少禁止提交真实凭证的警告注释 [.env.example:1] — 已修复：补充警告注释
- [x] [Review][Defer] `requirements.txt` 未锁定依赖版本 [requirements.txt] — deferred，待全部功能实现后 `pip freeze` 固定版本（架构文档明确说明此时间点）
- [x] [Review][Defer] 密码以明文字符串存储在 dict 中 [config/credentials.py] — deferred，本地工具架构设计决策，可接受；后续如有需要可引入 `SecretStr`
- [x] [Review][Defer] `reporter.py` 骨架使用 `pass` 而非 `NotImplementedError` [reporter.py] — deferred，设计选择；`pass` 不影响调用方，Story 1.4 实现
- [x] [Review][Defer] `validate.py` TODO 路径返回 0 退出码 [validate.py] — deferred，Story 5.1 实现完整调度逻辑时处理
- [x] [Review][Defer] `--source` 参数未校验数据源名称合法性 [validate.py] — deferred，Story 5.1 实现 choices 约束
- [x] [Review][Defer] `--source` 与 `--all` 同时传入无冲突检查 [validate.py] — deferred，Story 5.1 处理
- [x] [Review][Defer] `reporter.py` 未包含 `reports/` 目录创建保障 [reporter.py] — deferred，Story 1.4 实现时添加 `mkdir(parents=True, exist_ok=True)`
- [x] [Review][Defer] `.gitignore` 中额外排除 `reports/*-raw.md` 不在 AC 规范内 [.gitignore] — deferred，属合理实践但非 AC 要求；下一 Story 可统一确认

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

（无）

### Completion Notes List

- 创建完整项目骨架，全部 15 个文件/目录均按架构规范实现
- `config/credentials.py`：`get_credentials()` 完整骨架，含 13 个凭证键的校验逻辑
- `validate.py`：argparse CLI 骨架，`--source` / `--all` 参数可用，`--help` 正常输出
- `reporter.py`：`write_raw_report()` 和 `init_validation_report()` 函数签名完整，pass 实现待 Story 1.4 完善
- `config/field_requirements.yaml`：按报表分组，下划线命名（非连字符），含 profit_table 和 marketing_table 示例字段
- `tests/conftest.py`：`mock_credentials` fixture 包含全部 13 个测试凭证值
- 新增 `tests/test_structure.py`：38 个结构验证测试，RED→GREEN 全部通过
- `.gitignore` 排除了 `.env`、`__pycache__/`、`*.pyc`、`.pytest_cache/`

### File List

- validate.py（新建）
- reporter.py（新建）
- requirements.txt（新建）
- .env.example（新建）
- .gitignore（新建）
- README.md（新建）
- config/__init__.py（新建）
- config/credentials.py（新建）
- config/field_requirements.yaml（新建）
- sources/__init__.py（新建）
- reports/.gitkeep（新建）
- tests/__init__.py（新建）
- tests/conftest.py（新建）
- tests/fixtures/.gitkeep（新建）
- tests/test_structure.py（新建）
- _bmad-output/implementation-artifacts/1-1-项目结构初始化.md（更新状态）
- _bmad-output/implementation-artifacts/sprint-status.yaml（更新状态）
