# Story 5.2: 端到端集成验证

Status: done

## Story

作为操作者，
我希望能通过一个集成验证流程确认整个流水线的关键路径可正确运行，
以便在部署到新机器时快速确认工具可用性。

## Acceptance Criteria

1. mock 凭证和 mock source 模块（每个 source 返回预设样本数据），运行完整的 `--all` 调度流程后，所有 8 个 mock source 的 authenticate → fetch_sample → extract_fields → write_raw_report → init_validation_report 链路均被执行，`reports/` 目录下生成对应的 raw.md 和 validation.md 文件
2. 一个 mock source 的 `authenticate()` 返回 `False` 时，调度器跳过该数据源的 fetch_sample，记录为失败，继续处理下一个数据源，最终退出码为 `1`
3. 一个 mock source 的 `fetch_sample()` 抛出异常时，完整错误信息被记录到日志，该数据源标记为失败，其余数据源不受影响
4. `reports/{source}-validation.md` 已存在（模拟人工已标注内容）时，同一数据源再次运行后 raw.md 被覆盖更新，validation.md 内容保持不变（人工标注得到保护）
5. 运行 `pytest tests/ -m "not integration"` 时所有单元测试通过，无需真实 API 凭证或网络连接

## Tasks / Subtasks

- [x] 创建 `tests/test_e2e.py` 端到端集成测试文件 (AC: #1 #2 #3 #4 #5)
  - [x] 定义 `_make_mock_source()` 工厂函数（与 test_validate.py 中保持相同结构）
  - [x] 实现 AC1：`--all` 全链路测试 —— 使用 real reporter + tmp_path，验证 reports/ 下 raw.md 和 validation.md 均生成
  - [x] 实现 AC2：authenticate 返回 False —— 验证 fetch_sample 未调用、退出码 1、其余 source 继续
  - [x] 实现 AC3：fetch_sample 抛出异常 —— 验证其余 source 继续执行、退出码 1
  - [x] 实现 AC4：validation.md 保护 —— 预创建含"人工标注"的 validation.md，再次运行后内容不变
  - [x] 验证所有测试在 `pytest tests/ -m "not integration"` 下全部通过（不标记集成 marker）

## Dev Notes

### 核心实现约束

**本 Story 只新增一个文件：`tests/test_e2e.py`，其余文件均不修改。**

**与 test_validate.py 的关键区别：**

| 维度 | test_validate.py (Story 5.1) | test_e2e.py (Story 5.2) |
|------|------------------------------|-------------------------|
| reporter | 完全 mock（无文件 I/O） | 使用真实 reporter（patch REPORTS_DIR 到 tmp_path） |
| source 模块 | 完全 mock | 完全 mock |
| 目的 | 验证 validate.py 调度逻辑 | 验证 validate → reporter 全链路 + 文件生成 |

**关键 patch 路径（必须精确）：**

```python
# reporter.REPORTS_DIR 是模块级 Path 常量，需直接 patch 属性
import reporter as reporter_module
reporter_module.REPORTS_DIR = tmp_path / "reports"  # 在 fixture 或测试函数中设置

# 或使用 monkeypatch fixture（推荐）
monkeypatch.setattr(reporter_module, "REPORTS_DIR", tmp_path / "reports")

# validate.SOURCES / validate.get_credentials / validate.TRIPLEWHALE_TABLES
patch("validate.SOURCES", mock_sources)
patch("validate.get_credentials", return_value={})
patch("validate.TRIPLEWHALE_TABLES", ["pixel_orders_table"])  # 只用 1 张表加快速度
```

**reporter.REQUIREMENTS_PATH 也需 patch（避免读取真实 field_requirements.yaml）：**

```python
# reporter.py 中 REQUIREMENTS_PATH = Path("config") / "field_requirements.yaml"
# 在集成测试中可 patch 为 tmp_path 下不存在的文件（让 _load_field_requirements 返回 {}）
monkeypatch.setattr(reporter_module, "REQUIREMENTS_PATH", tmp_path / "nonexistent.yaml")
```

### 测试实现模式

**工厂函数（与 test_validate.py 保持一致）：**

```python
from unittest.mock import MagicMock

def _make_mock_source(
    auth_result: bool = True,
    sample: list | None = None,
    fields: list | None = None,
    fetch_exception: Exception | None = None,
    auth_exception: Exception | None = None,
) -> MagicMock:
    m = MagicMock()
    if auth_exception is not None:
        m.authenticate.side_effect = auth_exception
    else:
        m.authenticate.return_value = auth_result
    if fetch_exception is not None:
        m.fetch_sample.side_effect = fetch_exception
    else:
        m.fetch_sample.return_value = sample or [{"field": "value"}]
    m.extract_fields.return_value = fields or [
        {"field_name": "f", "data_type": "string", "sample_value": "v", "nullable": False}
    ]
    return m


def _make_all_mock_sources(override: dict | None = None) -> dict:
    sources = {
        "triplewhale": _make_mock_source(),
        "tiktok": _make_mock_source(),
        "dingtalk": _make_mock_source(),
        "youtube": _make_mock_source(),
        "awin": _make_mock_source(),
        "cartsee": _make_mock_source(),
        "partnerboost": _make_mock_source(),
        "social_media": _make_mock_source(),
    }
    if override:
        sources.update(override)
    return sources
```

**AC1 全链路测试骨架（real reporter + tmp_path）：**

```python
def test_full_pipeline_creates_report_files(self, tmp_path, monkeypatch) -> None:
    import reporter as reporter_module
    import validate
    monkeypatch.setattr(reporter_module, "REPORTS_DIR", tmp_path / "reports")
    monkeypatch.setattr(reporter_module, "REQUIREMENTS_PATH", tmp_path / "nonexistent.yaml")
    mock_sources = _make_all_mock_sources()
    with (
        patch("sys.argv", ["validate.py", "--all"]),
        patch("validate.SOURCES", mock_sources),
        patch("validate.get_credentials", return_value={}),
        patch("validate.TRIPLEWHALE_TABLES", ["pixel_orders_table"]),
        pytest.raises(SystemExit) as exc_info,
    ):
        validate.main()
    assert exc_info.value.code == 0
    reports_dir = tmp_path / "reports"
    # triplewhale 用真实 source_name，raw.md 以 source_name 命名
    for source_name in mock_sources:
        assert (reports_dir / f"{source_name}-raw.md").exists(), \
            f"{source_name}-raw.md 未生成"
        assert (reports_dir / f"{source_name}-validation.md").exists(), \
            f"{source_name}-validation.md 未生成"
```

**AC4 validation.md 保护测试骨架：**

```python
def test_validation_md_not_overwritten(self, tmp_path, monkeypatch) -> None:
    import reporter as reporter_module
    import validate
    monkeypatch.setattr(reporter_module, "REPORTS_DIR", tmp_path / "reports")
    monkeypatch.setattr(reporter_module, "REQUIREMENTS_PATH", tmp_path / "nonexistent.yaml")
    # 预先创建 validation.md，模拟人工已标注内容
    reports_dir = tmp_path / "reports"
    reports_dir.mkdir()
    human_annotation = "✅ 直接可用 — 人工已完成对标"
    (reports_dir / "tiktok-validation.md").write_text(human_annotation, encoding="utf-8")
    mock_sources = {"tiktok": _make_mock_source()}
    with (
        patch("sys.argv", ["validate.py", "--source", "tiktok"]),
        patch("validate.SOURCES", mock_sources),
        patch("validate.get_credentials", return_value={}),
        pytest.raises(SystemExit) as exc_info,
    ):
        validate.main()
    assert exc_info.value.code == 0
    # raw.md 已覆盖更新
    assert (reports_dir / "tiktok-raw.md").exists()
    # validation.md 内容保持人工标注不变
    content = (reports_dir / "tiktok-validation.md").read_text(encoding="utf-8")
    assert content == human_annotation, "validation.md 人工标注被覆盖！"
```

### 测试用例一览

| AC | 测试类/方法 | 验证点 |
|----|-----------|--------|
| AC1 | `TestFullPipeline::test_full_pipeline_creates_report_files` | 8 个 source 均生成 raw.md + validation.md，退出码 0 |
| AC1 | `TestFullPipeline::test_raw_md_contains_field_table` | raw.md 内容包含"实际返回字段"表格标题 |
| AC2 | `TestAuthFailure::test_auth_false_skips_fetch_and_marks_failed` | fetch_sample 未调用，退出码 1，其余 source 继续 |
| AC3 | `TestFetchException::test_fetch_exception_marks_failed_others_continue` | 异常 source 失败，其余继续，退出码 1 |
| AC4 | `TestValidationProtection::test_validation_md_not_overwritten` | raw.md 覆盖，validation.md 人工内容保留 |
| AC4 | `TestValidationProtection::test_raw_md_always_overwritten` | 相同 source 运行两次，raw.md 内容为最新（有时间戳差异） |
| AC5 | (自动) | 所有测试无 `@pytest.mark.integration` 标记，无需真实凭证 |

### 既有代码参照

**reporter.py 公开接口（已完整实现，不修改）：**
- `write_raw_report(source_name, fields, table_name, sample_count)` → 写 `REPORTS_DIR/{source}-raw.md`
- `init_validation_report(source_name)` → 仅首次创建 `REPORTS_DIR/{source}-validation.md`，`Path.exists()` 保护

**reporter.py 模块级常量（需 patch）：**
```python
REPORTS_DIR = Path("reports")           # → patch 到 tmp_path / "reports"
REQUIREMENTS_PATH = Path("config") / "field_requirements.yaml"  # → patch 到不存在的路径
```

**validate.py 模块级变量（需 patch）：**
```python
SOURCES: dict[str, Any]         # → patch 为 mock_sources
TRIPLEWHALE_TABLES: list[str]   # → patch 为 ["pixel_orders_table"]（加快速度）
# validate.get_credentials      # → patch 返回 {}
```

**tests/conftest.py 中已有 `mock_credentials` fixture（不使用，因为 test_e2e.py 直接 patch `validate.get_credentials`）**

### 潜在陷阱与防范

**1. reporter 模块级变量缓存问题**

reporter.REPORTS_DIR 在模块 import 时就已固化为 `Path("reports")`。使用 `monkeypatch.setattr` 而非 `patch()` 上下文管理器更可靠，因为 monkeypatch 保证测试结束后自动还原：

```python
# 推荐：monkeypatch.setattr（自动还原）
def test_xxx(self, tmp_path, monkeypatch):
    import reporter as reporter_module
    monkeypatch.setattr(reporter_module, "REPORTS_DIR", tmp_path / "reports")
```

**2. validate 模块缓存**

Python 会缓存已 import 的模块。在同一测试会话中多次 import validate 不会重新执行模块级代码（SOURCES 注册表）。因此 patch `validate.SOURCES` 必须在 `import validate` 之后，使用 `patch("validate.SOURCES", ...)` 而非 `patch.object`。

**3. triplewhale 特殊路由**

`_run_source("triplewhale", ...)` 会按 `TRIPLEWHALE_TABLES` 循环写 raw.md（每次覆盖同一文件 `triplewhale-raw.md`）。集成测试中将 `TRIPLEWHALE_TABLES` patch 为 `["pixel_orders_table"]`（单表），避免冗余 I/O，同时验证文件生成逻辑。

**4. social_media 在 --all 中始终失败**

`social_media` mock source 正常返回时退出码 0（AC1）；如果需要测试 social_media 的 NotImplementedError 行为，需在 AC3 测试中单独覆盖。

### Project Structure Notes

**新增文件：**
- `tests/test_e2e.py`：端到端集成测试（约 120-150 行，6-8 个测试用例）

**不修改任何现有文件：**
- `validate.py`、`reporter.py`、`config/credentials.py`、`sources/*.py`、`tests/conftest.py`、`tests/test_validate.py`

**测试命名规范（与项目保持一致）：**
- 文件：`tests/test_e2e.py`（snake_case）
- 类：`TestFullPipeline`、`TestAuthFailure`、`TestFetchException`、`TestValidationProtection`
- 方法：`test_<what>_<expected_behavior>`

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.2: 端到端集成验证] — AC 原文
- [Source: _bmad-output/planning-artifacts/architecture.md#Process Patterns] — 调度器错误处理模式
- [Source: _bmad-output/planning-artifacts/architecture.md#Structure Patterns] — `reports/` 目录规范
- [Source: _bmad-output/implementation-artifacts/5-1-validate.py-cli-入口与调度器.md#Dev Notes] — validate.py 调度逻辑、测试模式、mock 路径
- [Source: validate.py] — 当前完整实现（SOURCES 注册表、_run_source、main）
- [Source: reporter.py] — REPORTS_DIR 常量、write_raw_report、init_validation_report
- [Source: tests/test_validate.py] — _make_mock_source 工厂、_make_all_mock_sources、mock 路径示例

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

无调试问题。10 个测试用例全部一次通过，无需修改。

### Completion Notes List

- 新增 `tests/test_e2e.py`：10 个端到端集成测试用例，覆盖全部 5 条 AC
- 与 test_validate.py 的关键区别：使用真实 reporter（patch REPORTS_DIR 到 tmp_path），验证完整 validate → reporter → 文件 全链路
- 关键 patch：`monkeypatch.setattr(reporter_module, "REPORTS_DIR", ...)` 和 `REQUIREMENTS_PATH`
- 测试结构：5 个测试类（TestFullPipeline / TestAuthFailureIsolation / TestFetchExceptionIsolation / TestValidationReportProtection / TestNoRealCredentialsRequired），共 10 个测试方法
- 回归：273/274 非集成测试通过，1 个预存失败（triplewhale.py 注释含 os.getenv 字符串，与本 Story 无关，已知问题）

### File List

- tests/test_e2e.py（新增）

### Review Findings

- [x] [Review][Patch] AC2: `test_auth_false_skips_fetch_and_marks_failed` 未断言 `dingtalk-validation.md` 不存在 [tests/test_e2e.py:187]
- [x] [Review][Patch] AC2/继续执行: `test_sources_after_auth_failure_continue` 缺少 `social_media` 在继续执行断言中 [tests/test_e2e.py:213]
- [x] [Review][Patch] AC3: `test_fetch_exception_marks_source_failed` 缺少 `caplog` 断言"完整错误信息被记录到日志" [tests/test_e2e.py:225]
- [x] [Review][Defer] 每个测试方法重复5行 setup 代码，应提取为 fixture [tests/test_e2e.py] — deferred, pre-existing（与 test_validate.py 保持一致的既有模式）
- [x] [Review][Defer] import 语句写在测试方法体内而非模块级 [tests/test_e2e.py] — deferred, pre-existing（与 test_validate.py 既有风格一致）
- [x] [Review][Defer] `_make_all_mock_sources` 硬编码8个 source 名称 [tests/test_e2e.py:47] — deferred, pre-existing（与 test_validate.py 保持一致）
- [x] [Review][Defer] `--all` 测试中 social_media 使用正常 mock，未体现真实 NotImplementedError [tests/test_e2e.py:47] — deferred, pre-existing（Dev Notes 明确设计决策）
- [x] [Review][Defer] AC4 未覆盖 triplewhale 多表场景下 validation.md 保护 — deferred, pre-existing（超出规格要求的额外覆盖）
- [x] [Review][Defer] AC2/AC3 未测试 `authenticate()` 直接抛异常场景 — deferred, pre-existing（AC2 规格仅要求返回 False）
